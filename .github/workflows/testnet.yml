name: Portal Bridge [Testnet]

on: 
  pull_request:
#    paths:
#     - '!apps/token-list/**'

jobs:
  branch-sha:
    name: Compute Branch Sha
    runs-on: ubuntu-latest
    concurrency:
        group: ${{ github.ref }}-testnet-sha
    outputs:
      value: ${{ steps.set-version.outputs._BRANCH_NAME_SHA }}
    steps:
      - name: Set Version
        id: set-version
        run: echo "_BRANCH_NAME_SHA=$(echo ${{ github.event.pull_request.head.ref }} | sha256sum | cut -c -10)" >> "${GITHUB_OUTPUT}"
  build:
    name: "Build Testnet"
    uses: ./.github/workflows/build.yml
    with:
      name: testnet
      separator: T
      connect-branch: production@0.2.7
      advanced-tools-branch: advanced-tools
      environment: Cloudflare-Testnet
      public-url: "/${{ needs.branch-sha.outputs.value }}"
      project-branch: ${{ github.event.pull_request.head.ref }}
      app-domain: testnet.portalbridge.com
    secrets: inherit
    needs: 
      - branch-sha
  summary:
    name: "Summary"
    needs:
      - build
      - branch-sha
    runs-on: "ubuntu-latest"
    environment: Cloudflare-Preview
    concurrency:
      group: ${{ github.ref }}-summary-preview
      cancel-in-progress: true
    steps:
      - name: Write Summary
        run: |
          echo "The preview URL is: ${{ vars.TESTNET_ROOT_URL }}/${{ needs.branch-sha.outputs.value }}/" - ${{ needs.build.outputs.pkg-version }}>> ${GITHUB_STEP_SUMMARY}
    